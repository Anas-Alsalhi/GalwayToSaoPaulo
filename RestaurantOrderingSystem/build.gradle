dependencies {
    // ...existing dependencies...
    // Removed JUnit dependencies
    // ...existing dependencies...
}

sourceSets {
    main {
        resources {
            srcDirs = ['src']
            include '**/*.properties'
        }
    }
}
